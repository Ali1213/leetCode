package  main
func allPathsSourceTarget(graph [][]int) [][]int {
    ans := make([][]int, 0)
    bfs := make([][]int, 0)
    bfs = append(bfs, []int{0})

    for ; len(bfs) > 0 ; {
        cur := bfs[0]
        
    }
}